## Overview

This project converts jscad-planner objects into STEP files.

You should look at the `./references/jscad-to-gltf` project for inspiration.

## Coding

- Use `bun`/`bunx` instead of node
- Create tests with png snapshot testing
- One test per file
